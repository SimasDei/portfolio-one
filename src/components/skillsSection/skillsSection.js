import React from 'react';
import Grid from '@material-ui/core/Grid';

export default () => {
  return (
    <Grid container className="skillsContainer">
      <section id="skills" className="skillsSection">
        <div>
          <h1>Ima a skills Section, Look at me !</h1>
        </div>
      </section>
    </Grid>
  );
};
